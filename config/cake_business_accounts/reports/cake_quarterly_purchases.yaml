name: cake_quarterly_purchases
label: Quarterly purchases
route: /reports/cake-quarterly-purchases
queries:
  - id: purchases
    model: cake_expense
    measures: [cake_expense_sum_total_cost, count]
    dimensions: [item]
visuals:
  - id: spend_by_item
    type: bar
    source: purchases
    x_field: item
    series:
      - field: cake_expense_sum_total_cost
        label: Total spend
        type: bar
        format: currency
